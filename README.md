# install
## Pasos a seguir
- primer paso: Crear variables de entorno

    ```bash
    cp .env.sample .env
    ```

- segundo paso: instalar node_modules
    ```
    npm install
    ```

- tercer paso modo produccion
    ```
    npm run star
    ```
